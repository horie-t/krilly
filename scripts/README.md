# scripts/

実機ブリングアップ・キャリブレーション・テレオペ用の単体スクリプト置き場。

各マイルストーンで追加予定:

- **M1**: `motor_spin.py`（L6470 単体/3輪回転）, `imu_stream.py`（BNO055 方位表示）, `red_detect.py`（カメラ赤検出オーバーレイ）
- **M2**: `teleop.py`（キーボード→速度指令）, `calibrate.py`（距離・L 補正）

スクリプトは `python -m` で実行できるよう、パッケージ機能を薄く呼び出す形にする。
